package org.example.constants;

import java.util.Arrays;
import java.util.List;

public class ExcelConstants {

    // Define the list of expected headers as a constant
    public static final List<String> EXPECTED_HEADERS = Arrays.asList(
            "Branch Name", "Policy Number", "Application No", "Business Type",
            "LA Name", "Plan Name", "Premium Amount", "Payment Mode",
            "Ext Ref No", "Cod Drawer Acct"
    );
}